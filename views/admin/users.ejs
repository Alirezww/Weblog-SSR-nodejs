<% if (users.docs.length > 0) { %>
    <h2 class="mt-2">آخرین پست ها</h2>
    <div class="table-responsive">
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th>#</th>
                    <th>عنوان</th>
                    <th>تاریخ</th>
                    <th>ایمیل</th>
                </tr>
            </thead>
            <tbody>
                <% for (let user of users.docs) { %>
                    <tr>
                        <td>1</td>
                        <td>
                            <a href="/blogs/<%= user._id %>"></a>
                            <%= user.fullname %>
                        </td>
                        <td>
                            <%= formateDate(user.createdAt) %>
                        </td>
                        <td>
                            <%= user.email %>
                        </td>
                        <td>
                            <a href="/dashboard/edit-user/<%= user._id %>" class="btn btn-sm btn-primary">ویرایش</a>
                        </td>
                        <td>
                            <a href="/dashboard/delete-user/<%= user._id %>" class="btn btn-sm btn-danger">حذف</a>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
        <%- include("../includes/paginatio.ejs", {pagination : users}) %>
    </div>
    <% } else { %>
        <div class="text-center">
            <h3 class="mt-2">
                در حال حاضر هیچ پستی توسط شما در پایگاه داده ثبت نشده است
            </h3>
        </div>
        <% } %>

            <p>hi to All</p>